version: '3.8'

services:
  portfolio-chatbot:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./data:/app/data
      - ./faiss_index:/app/faiss_index
